<ul>
	<li *ngFor="let task of all_tasks">
	<span>{{task.id}}</span>
	<input type="checkbox" class="hidden-box" checked="checked" *ngIf="task.isComplete"/>
	<input type="checkbox" *ngIf="task.isComplete==false"/>
	<span class="time">{{task.time}}</span>
	<span>{{task.name}}</span>
	</li>
</ul>

